<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Project Full - ISLP Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    body {
      background-color: #f8f9fa;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
    }

    .full-container {
      background-color: white;
      border-radius: 20px;
      padding: 40px;
      text-align: center;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
      max-width: 500px;
      width: 90%;
    }

    .full-icon {
      font-size: 4rem;
      color: #dc3545;
      margin-bottom: 20px;
    }

    .full-title {
      font-size: 2rem;
      font-weight: bold;
      color: #dc3545;
      margin-bottom: 20px;
    }

    .full-message {
      font-size: 1.1rem;
      color: #666;
      margin-bottom: 30px;
      line-height: 1.6;
    }

    .project-info {
      background-color: #f8f9fa;
      border-radius: 10px;
      padding: 20px;
      margin-bottom: 30px;
      text-align: left;
    }

    .project-info h5 {
      font-weight: bold;
      margin-bottom: 15px;
    }

    .member-info {
      background-color: #fff3cd;
      border: 1px solid #ffeaa7;
      border-radius: 8px;
      padding: 15px;
      margin-bottom: 20px;
    }

    .btn-custom {
      background-color: #007bff;
      color: white;
      padding: 12px 30px;
      border: none;
      border-radius: 25px;
      font-weight: 500;
      text-decoration: none;
      display: inline-block;
      margin: 0 10px;
      transition: background-color 0.3s ease;
    }

    .btn-custom:hover {
      background-color: #0056b3;
      color: white;
      text-decoration: none;
    }

    .btn-secondary {
      background-color: #6c757d;
      color: white;
      padding: 12px 30px;
      border: none;
      border-radius: 25px;
      font-weight: 500;
      text-decoration: none;
      display: inline-block;
      margin: 0 10px;
      transition: background-color 0.3s ease;
    }

    .btn-secondary:hover {
      background-color: #545b62;
      color: white;
      text-decoration: none;
    }

    .student-badge {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background-color: blue;
      color: white;
      padding: 8px 16px;
      border-radius: 20px;
      font-size: 0.9rem;
      font-weight: 500;
      z-index: 1000;
    }
  </style>
</head>
<body>

  <div class="full-container">
    <!-- Icon -->
    <div class="full-icon">
      <i class="fas fa-users-slash"></i>
    </div>

    <!-- Title -->
    <h1 class="full-title">Project is Full!</h1>

    <!-- Message -->
    <div class="full-message">
      Sorry, this project has reached its maximum capacity and is no longer accepting new members.
    </div>

    <!-- Project Information -->
    <div class="project-info">
      <h5><%= project.project_title %></h5>
      <p><strong>Description:</strong> <%= project.description %></p>
      <p><strong>Start Date:</strong> <%= new Date(project.project_start).toLocaleDateString('en-GB') %></p>
      <p><strong>End Date:</strong> <%= new Date(project.project_end).toLocaleDateString('en-GB') %></p>
    </div>

    <!-- Member Count Info -->
    <div class="member-info">
      <strong>Current Members:</strong> <%= currentMemberCount %> / <%= maxMembers %>
      <br>
      <small>This project has reached the maximum limit of <%= maxMembers %> members.</small>
    </div>

    <!-- Action Buttons -->
    <div class="mt-4">
      <a href="/ISLP/<%= project.projectid %>" class="btn-custom">View Project Details</a>
      <a href="/student" class="btn-secondary">Back to Dashboard</a>
    </div>

    <!-- Suggestion -->
    <div class="mt-4">
      <small class="text-muted">
        You can still view the project details or explore other available projects on your dashboard.
      </small>
    </div>
  </div>

  <!-- Font Awesome for icons -->
  <script src="https://kit.fontawesome.com/your-kit-id.js" crossorigin="anonymous"></script>

  <!-- Username badge -->
  <div class="student-badge">
    <% if (currentUser && currentUser.username) { %>
      <%= currentUser.username %>
    <% } else { %>
      Student
    <% } %>
  </div>

</body>
</html>